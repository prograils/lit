var $row = $('td.localization_row[data-id="<%= @localization.id %>"]');
$row.data('editing', 0);
$row.data('content', "<%= ejs @localization.get_value %>");
$row.html("<%= @localization.get_value %>");